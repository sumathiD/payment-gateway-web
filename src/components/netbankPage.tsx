import { Card } from 'antd'
import React from 'react'

const NetbankPage = () => {
  return (
    <Card title="NET BANKING" bordered={false} style={{ width: 700, color: "#040748" }}>
      <button className="preferredpage">PAY ₹200</button>

    </Card>
  )
}

export default NetbankPage